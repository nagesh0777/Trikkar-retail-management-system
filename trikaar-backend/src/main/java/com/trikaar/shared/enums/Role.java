package com.trikaar.shared.enums;

/**
 * System-wide role definitions.
 * Roles determine access level across all modules.
 */
public enum Role {
    ADMIN,
    ACCOUNTANT,
    WORKER,
    ANALYST
}
